#include <stdio.h>
int main() {
    int i,N;
    long long int X;

    scanf("%d", &i);
    while(i--){
        scanf("%d",&N);
        scanf("%d",&X);
        int A[N];
        for(int j=0;j<=N;j++){
            scanf("%d",&A[j]);
        }
    }

    return 0;
}
